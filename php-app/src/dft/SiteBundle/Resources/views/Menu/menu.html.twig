{% include 'dftSiteBundle:Common:top.html.twig' %}

{# Include opening time and contact. #}
{% include 'dftSiteBundle:Common:opening-time-contact.html.twig' %}

<!-- Header -->
<header class="header_bg clearfix">
    <div class="top-menu clearfix">
        {# Include menu logo. #}
        {% include 'dftSiteBundle:Common:menu-logo.html.twig' %}
        {# Include top menu. #}
        {% include 'dftSiteBundle:Common:top-menu.html.twig' %}
    </div>
</header>
<!-- /Header -->

<div class="clear"></div>


<section class="banner_about_bg clearfix">

    <header class="container clearfix">
        <div class="banner_about">
            <h1>Menu</h1>
            <p class="text-white">Home > Menu</p>
        </div>

        <div class="clear"></div>
    </header>
</section>

<div class="clear padding40"></div>

<section class="container clearfix">
    <div class=" menu_list small_screen">
        <select name="menu" class="hidden_list">
            {% for category in menu_item_categories %}
                <option value="{{ category.category_name }}"><a href="">{{ category.category_name }}</a></option>
            {% endfor %}
        </select>
    </div>
</section>

{% if restaurant_closed %}
    <div style="width: 100%; font-weight: bold; font-size: 23px; text-align: center;">Our restaurant is now closed. We will be open between {{ restaurant_settings.opening_time }} and {{ restaurant_settings.closing_time }}.</div>
    <br/>
{% endif %}
{% if lunch_break %}
    <div style="width: 100%; font-weight: bold; font-size: 23px; text-align: center;">Our team is on lunch break between {{ restaurant_settings.lunch_break_start }} and {{ restaurant_settings.lunch_break_end }}.</div>
    <br/>
{% endif %}

<section class="container clearfix">

    <div class="menu_list full_screen">
        <div class="clearfix">
            <!--------left area--------->
            <h2>Our Menu</h2>
            <h1 class="regular brown bottom_line"></h1>
            <div class="menu_cat">
                <ul>
                    {% for category in menu_item_categories %}
                        <li><a href="/menu/{{ category.url }}">{{ category.category_name }}</a></li>
                    {% endfor %}
                </ul>
            </div>

        </div>
    </div>

    <div class="menu_detail">

        <div class="clearfix">
            <!--------left area--------->
            <h2>{{ selected_category_id ? selected_category_id | replace({ '-': ' ' }) : 'All menu items' }}</h2>
            <h1 class="regular brown bottom_line"></h1>

        </div>

        <div class="clear padding{% include 'dftSiteBundle:Common:top.html.twig' %}

{# Include opening time and contact. #}
{% include 'dftSiteBundle:Common:opening-time-contact.html.twig' %}

<!-- Header -->
<header class="header_bg clearfix">
        <div class="container clearfix">
            {# Include menu logo. #}
            {% include 'dftSiteBundle:Common:menu-logo.html.twig' %}
            {# Include top menu. #}
            {% include 'dftSiteBundle:Common:top-menu.html.twig' %}
        </div>
        </header>
        <!-- /Header -->

        <div class="clear"></div>

        <section class="banner_contact_bg clearfix">

            <header class="container clearfix">
                <div class="banner_contact">
                    <h1>Contact Us</h1>
                    <p class="text-white">Home > Contact Us</p>
                </div>

                <div class="clear"></div>
            </header>
        </section>

        <!-- START CONTENT -->
        <section class="container clearfix">
            <!-- START PAGE INFO -->
            <header class="container page_info clearfix">
                <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script><div style="overflow:hidden;height:280px;width:100%;"><div id="gmap_canvas" style="height:280px;width:100%;"></div><a class="google-map-code" href="http://www.addmap.org" id="get-map-data">www.addmap.org</a><style>#gmap_canvas img{max-width:none!important;background:none!important}</style><a class="google-map-data" href="http://www.stromleo.de" id="get-map-data">http://www.stromleo.de</a></div><script type="text/javascript"> function init_map(){var myOptions = {zoom:14,center:new google.maps.LatLng(40.8054567,-73.96547470000002),mapTypeId: google.maps.MapTypeId.ROADMAP};map = new google.maps.Map(document.getElementById("gmap_canvas"), myOptions);marker = new google.maps.Marker({map: map,position: new google.maps.LatLng(40.8054567, -73.96547470000002)});infowindow = new google.maps.InfoWindow({content:"<b>The Breslin</b><br/>2880 Broadway<br/> New York" });google.maps.event.addListener(marker, "click", function(){infowindow.open(map,marker);});infowindow.open(map,marker);}google.maps.event.addDomListener(window, 'load', init_map);</script>
                <h1 class="regular brown bottom_line"></h1>

                <div class="clear"></div>
            </header>

            <!-- END PAGE INFO -->

            <div class="padding15"></div>

            <div class="contact">
                <!-- START COL 1/3 -->
                <div class="take_away">

                    <h2>Take Away info</h2>
                    <div class="padding20"></div>

                    <p>Take Away Service Available
                        Cheques & Credit Cards Are Not Accepted Under £10.00
                    </p>
                    <div class="padding20"></div>
                    <p>Address: 8 Brick Lane, Bangla Town,<br  />
                        London E1 6RL <br  />
                        <br  />
                        Telephone:  02072476606 , 73778767</p>

                    <div class="clear"></div>

                </div>
                <!-- END COL 1/3 -->

                <!-- START COL 2/3 LAST -->
                <div class="fill_form">

                    <h2>Contact Form</h2>

                    <div class="padding20"></div>


                    <!-- START CONTACT FORM -->
                    <form action="#" class="contact_form"  id="myForm">
                        <ul>
                            <li>
                                <input class="inputText" type="text" id="name" name="name" placeholder="Name:" />
                            </li>

                            <li>
                                <input class="inputText" type="text" id="email" name="email" placeholder="Email:" />
                            </li>

                            <li>
                                <input class="inputText" type="text" id="website" name="website" placeholder="Phone:" />
                            </li>

                            <li>
                                <textarea class="inputTextarea" cols="88" rows="6" id="message" name="message" placeholder="Message:" ></textarea>
                            </li>
                            <div class="clear"></div>
                            <li class="submit">
                                <a href="javascript:void(0);" class="button" onclick="myFunction();">Reset</a>
                            </li>

                            <li class="submit">
                                <a href="javascript:void(0);" class="button" onclick="$('.contact_form').submit();">Send</a>
                            </li>

                        </ul>
                    </form>

                    <script>
                        function myFunction()
                        {
                            document.getElementById("myForm").reset();
                        }
                    </script>
                    <!-- END CONTACT FORM -->

                </div>
                <!-- END COL 2/3 LAST -->

            </div>

            <div class="clear padding20"></div>


        </section>
        <!-- END CONTENT -->

        <div class="clear padding40"></div>

        {# Footer. #}
        {% include 'dftSiteBundle:Common:footer.html.twig' %}

        {# Bottom. #}
        {% include 'dftSiteBundle:Common:bottom.html.twig' %}40"></div>

    {% if menu_items.data is defined %}
        {% for menu_item in menu_items.data %}
            <div class="menu-h4-left"><h4>{{ menu_item.item_name }}</h4></div><div class="menu-h4-right"><h3>£{{ menu_item.price }}</h3><a href="?cart_add_item={{ menu_item.id }}"><img src="/images/icon_cart_in.png" /></a></div>

            {% if menu_item.item_description %}
                <div class="clear padding10"></div>
                <p>{{ menu_item.item_description }}</p>
            {% endif %}
            <div class="clear padding40"></div>
        {% endfor %}
    {% endif %}
    </div>

    {% if discounts.generic|length > 0 or discounts.option|length > 0 %}
        <div class="cart_box cart_hide" style="margin-bottom: 30px !important;">
            <div>
                <h4>Discounts</h4>
                <h1 class="regular brown bottom_line"></h1>
            </div>
            <div class="clear"></div>

            <div class="box_heading">
                <form>
                    <table>
                        {% for discount in discounts.generic %}
                            <tr>
                                <td><input type="checkbox" checked disabled /></td>
                                <td style="text-align: left; padding-left: 3px; font-family: 'Open Sans', sans-serif; font-size: 12px;">{{ discount.discount_name }}</td>
                            </tr>
                        {% endfor %}
                        {% if discounts.option|length > 0 %}
                            <tr>
                                <td colspan="2">Select a discount and add the menu item to your shopping cart.</td>
                            </tr>
                            <tr>
                                <td><input {{ option_discount_id == 0 ? 'checked' : '' }} type="radio" name="discount" value="0" /></td>
                                <td style="text-align: left; padding-left: 3px; font-family: 'Open Sans', sans-serif; font-size: 12px;">None</td>
                            </tr>
                        {% endif %}
                        {% for discount in discounts.option %}
                            <tr>
                                <td><input {{ option_discount_id == discount.id ? 'checked' : '' }} type="radio" name="discount" value="{{ discount.id }}" /></td>
                                <td style="text-align: left; padding-left: 3px; font-family: 'Open Sans', sans-serif; font-size: 12px;">{{ discount.discount_name }}</td>
                            </tr>
                        {% endfor %}
                    </table>
                </form>
            </div>
        </div>
        <script language="JavaScript">
            function discountSelectionChange(event) {
                document.location = document.location + "?discount_id=" + event.target.value;
            }
            $('input[name=discount]:radio').change(discountSelectionChange);
        </script>
    {% endif %}

    <div class="cart_box cart_hide">
        <div>
            <h4>Your Cart</h4>
            <h1 class="regular brown bottom_line"></h1>
        </div>
        <div class="clear"></div>

        <div style="text-align: center; width: 100%;">
            <br/>
            <noscript>
                <div name="enablejavascript" id="enablejavascript" style="font-weight: bold;">
                    For full functionality of this site it is necessary to enable JavaScript.
                    Here are the <a href="http://www.enable-javascript.com/" target="_blank">
                        instructions how to enable JavaScript in your web browser</a>.
                </div>
            </noscript>
            <select name="delivery_type" id="delivery_type" class="inputText resetPassEmailInput" style="font-size: 12px; width: 240px;" >
                <option value="1" {{ delivery_type == 1 ? "selected" : '' }}>Delivery (Est: {{ restaurant_settings.default_delivery_time }} minutes)</option>
                <option value="2" {{ delivery_type == 2 ? "selected" : '' }}>Collection (Est: {{ restaurant_settings.default_collection_time }} minutes)</option>
            </select>
            <script language="JavaScript">
                // Handle delivery type changes.
                $('#delivery_type').change(function() {
                    document.location = document.location + "?delivery_type=" + $('#delivery_type').val();
                });
            </script>
        </div>

        {% if delivery_type == 1 and restaurant_settings.minimum_website_order_value %}
            <div style="font-weight: bold; text-align: center; width: 100%;">Minimum order: {{ restaurant_settings.minimum_website_order_value }} GBP</div>
        {% endif %}
        {% if shopping_cart_items|length > 0 %}
            <div class="box_heading">
                <ul>
                    <li>Dish Name</li>
                    <li>Qty.</li>
                    <li>Price</li>
                    <li></li>
                </ul>
            </div>

            <div class="clear"></div>

            <div class="dishes">
                {# Prepare a total price variable, and display items. #}
                {% set total_price = 0 %}
                {% for item in shopping_cart_items %}
                    {# Compute total price. #}
                    {% set total_price = total_price + item.price %}
                    <ul>
                        <li>{{ item.name|length > 15 ? item.name|slice(0, 15) ~ '...' : item.name  }}</li>
                        <li>{{ item.count }}</li>
                        <li>£{{ item.price }}</li>
                        <li><a href="?cart_remove_item={{ item.id }}"><img src="/images/pricing_remove.png" alt="" /></a></li>
                    </ul>
                {% endfor %}
                <ul>
                    <li><b>TOTAL</b></li>
                    <li><b>{{ shopping_cart_item_count }}</b></li>
                    <li><b>£{{ total_price }}</b></li>
                    <li>&nbsp;</li>
                </ul>
            </div>

            <div class="clear"></div>

            {% if not lunch_break and not restaurant_closed and (delivery_type == 2 or total_price >= restaurant_settings.minimum_website_order_value) %}
                <div class="order_tbn"><a href="/cart/">ORDER NOW</a></div>
            {% endif %}
        {% else %}
            <div class="box_heading" style="width: 100%; text-align: center;">Your shopping cart is empty.</div>
        {% endif %}


    </div>

</section>
<!--------right area end--------->


<div class="clear padding40"></div>

{# Footer. #}
{% include 'dftSiteBundle:Common:footer.html.twig' %}

{# Bottom. #}
{% include 'dftSiteBundle:Common:bottom.html.twig' %}